# 作業說明

---

## 習題一

[習題一](https://github.com/JoanWeng/_sp/tree/master/01) 透過 opencode 參考 cccmit/08-comment_v1 製作出一個用 C 語言寫的精簡編譯器加虛擬機。把 p0 語言編譯成四元組中間碼，再交給內建 VM 執行。四元組每條指令有 op、arg1、arg2、result 四個欄位，透過回填技術處理條件跳轉（先留空位等 body 解析完再補上）。函數呼叫則用 Frame stack 實作，每次 CALL 就 sp++ 建立新 Frame，每個 Frame 有獨立的變數空間和返回地址，所以 factorial 這類遞迴函式能正確運作。
[參考資料](https://github.com/ccc114b/cpu2os/tree/master/02-%E7%B3%BB%E7%B5%B1%E7%A8%8B%E5%BC%8F/_books/_code/02-compiler/08-comment_v1)

---

## 習題二

[習題二](https://github.com/JoanWeng/_sp/tree/master/02) 透過 Google AI Studio Gemini 2.5 Pro Preview 製作了一個用 Emoji 當關鍵字的程式語言 EmoLang。採用樹狀走訪直譯模型——Parser 建好 AST 後 Evaluator 直接遞迴走訪執行，沒有中間碼。最大特色是零相依架構，完全不依賴 C 標準庫，直接透過 sys_write、sys_read 等 POSIX 系統呼叫處理 I/O，記憶體用 Arena Allocator 管理（預先開好陣列，不用 malloc）。指標是本質上是虛擬陣列的索引，不會有 segfault。
[對話連結](https://aistudio.google.com/app/prompts?state=%7B%22ids%22:%5B%221QvoQLuULjVclSqiMU1nEd_ct2cIslc7J%22%5D,%22action%22:%22open%22,%22userId%22:%22117223938390670233296%22,%22resourceKeys%22:%7B%7D%7D&usp=sharing)

---

## 習題三

[習題三](https://github.com/JoanWeng/_sp/tree/master/03) 透過 opencode 用 Python 重新實作 EmoLang（v4.2），跟 02 共用同一套 Emoji 語法，但功能更完整。新增了 Tkinter GUI 編輯器（語法突顯、行號、快捷鍵插入 Emoji）和 LSP 伺服器（語法檢查、程式碼補全），Parser 還有診斷模式可以一次收集所有錯誤而不會中途停掉。同一套語言規格用 C 和 Python 分別實作，風格完全不同——C 追求底層控制與零相依，Python 追求開發速度與功能完整度，讓我理解技術選擇取決於目標場景。

---

## 習題四

[習題四](https://github.com/JoanWeng/_sp/tree/master/04) 透過 Claude.ai 編寫了一份 12 章的系統程式教材，從硬體底層（暫存器與旗標、定址模式、ISA）到程式語言層（組合語言、堆疊與副程式、巨集處理器）再到工具鏈（組譯器設計、連結器與載入器、編譯器概論），最後涵蓋作業系統介面與開發工具實務。每章都有對應的 Python 習題可以動手驗證，例如模擬兩遍組譯、計算有效位址或寫迷你編譯器，理論和實作搭配得很好。
[對話連結](https://claude.ai/share/2f1c07fd-8ce8-43e5-953b-422a39893a12)

---

## 習題五

[習題五](https://github.com/JoanWeng/_sp/tree/master/05) 透過 opencode 製作了執行緒同步相關的程式碼，包含三個經典主題。銀行存提款模擬展示 mutex 如何解決 race condition（balance++ 在 CPU 層級是讀取-加一-寫回三步驟，不加鎖會 lost update）。生產者消費者問題使用 mutex + condition variable 搭配 ring buffer，用 while 而非 if 檢查條件以防止 spurious wakeup。哲學家用餐問題則透過固定資源取得順序（叉子編號小到大）打破 circular wait 來預防 deadlock。學到了 Coffman 四條件是 deadlock 的必要條件，打破任何一個就能預防。

---

## 習題六

[習題六](https://github.com/JoanWeng/_sp/tree/master/06) 透過 opencode 製作了一系列關於 Linux 行程管理與檔案 I/O 的範例程式。行程管理部分包含 fork 的基本分叉行為（一次 fork 變 2 個、兩次 fork 變 4 個）、父子行程變數隔離（子改變數不影響父）、fork + exec 經典搭配（子行程取代為新程式）、wait 回收子行程避免殭屍。檔案 I/O 部分涵蓋 open/read/write/close 基本操作、dup2 重新導向 stdout 到檔案、pipe + dup2 實作 `ls | wc` 管線。
